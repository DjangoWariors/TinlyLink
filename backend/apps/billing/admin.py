"""
Admin configuration for billing app.
Billing uses Stripe directly, no local models.
"""

from django.contrib import admin

# Billing uses Stripe for all data storage
# No local models to register
